import { CommaSeperatePipe } from './comma-seperate.pipe';

describe('CommaSeperatePipe', () => {
  it('create an instance', () => {
    const pipe = new CommaSeperatePipe();
    expect(pipe).toBeTruthy();
  });
});
